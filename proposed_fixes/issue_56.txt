=== REPORT START ===
Original: testing_chat/stats.py
Fixed: fixed_code.txt
Status: Success
Line: 15
Why: The original code incorrectly returned the upper middle element for even-length lists. The fix adds a conditional check:
Patch:
def median(nums):
    """
    Return the median of a non-empty list of numbers.
    BUG: For even-length lists, this incorrectly returns the upper middle
    element instead of the average of the two middle elements.
    """
    if not nums:
        raise ValueError("nums must be non-empty")

    s = sorted(nums)
    n = len(s)
    mid = n // 2

    # ❌ BUG: even-length case should average s[mid-1] and s[mid]
    if n % 2 == 0:
        return (s[mid - 1] + s[mid]) / 2
    else:
        return s[mid]
Timestamp: 2025-09-28T14:21:09.513853
=== REPORT END ===

